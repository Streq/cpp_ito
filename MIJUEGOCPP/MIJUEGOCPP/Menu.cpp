#include "Menu.h"
/*


Menu::Menu()
{
}

void Menu::move_up(){
	cursor = (cursor + buttons.size() - 1) % buttons.size();
}

void Menu::move_down(){
	cursor = (cursor + buttons.size() + 1) % buttons.size();
}

void Menu::execute(unsigned index) {
}

void Menu::go_in() {
	execute(cursor);
}

void Menu::go_back(){
}
*/