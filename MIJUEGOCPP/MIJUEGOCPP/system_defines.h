#pragma once
#define COMPONENTS_MOVEMENT \
X(Movement)\
X(Position)

#define COMPONENTS_RENDERING \
X(Rendering)\
X(Position)

#define COMPONENTS_CONTROLLER \
X(Controller)\
X(State)

#define COMPONENTS_COLLISION \
X(CollisionBody)\
X(CollisionInfo)\
X(Position)

#define COMPONENTS_STATE \
X(State)


#define COMPONENTS_TIME \
X(TimeSpan)

#define COMPONENTS_HEALTH \
X(Health)